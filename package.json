{
  "name": "cloud-event-driven-ecommerce",
  "description": "Event-driven e-commerce backend built with serverless architecture using AWS services like Lambda, SNS, and SQS.",
  "module": "app.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build:lambdas": "rm -rf ./dist && bun build ./src/presentation/**/*.ts --outdir ./dist --target=node --format=cjs",
    "dev": "bun run build && cd ./sam && sam local start-api --template template.yml"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.161",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}